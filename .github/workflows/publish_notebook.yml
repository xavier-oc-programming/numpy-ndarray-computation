name: Publish Notebook

on:
  push:
    branches:
      - main
    paths:
      - 'notebooks/analysis/A_01_NumPy_Exercises.ipynb'

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install jupyter nbconvert

      - name: Convert notebook to HTML
        run: |
          jupyter nbconvert --to html \
            --no-input \
            --output index.html \
            notebooks/analysis/A_01_NumPy_Exercises.ipynb

      - name: Move to notebook directory
        run: |
          mkdir -p notebook
          mv index.html notebook/index.html

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: notebook
          destination_dir: notebook
          keep_files: true
